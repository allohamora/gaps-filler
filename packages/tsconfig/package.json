{
  "name": "@gaps-filler/tsconfig",
  "private": true,
  "scripts": {
    "format": "prettier . --check",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@gaps-filler/prettier-config": "*",
    "prettier": "^3.5.3"
  },
  "prettier": "@gaps-filler/prettier-config",
  "lint-staged": {
    "*.json": "prettier --write"
  }
}
